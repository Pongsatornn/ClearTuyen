/**
 * ชื่อโมเดล Groq ที่ใช้ทั้งโปรเจกต์ — รวมไว้ที่เดียว
 *
 * เหตุผลที่ต้องมีไฟล์นี้: **Groq ถอดโมเดลออกจากบริการโดยไม่แจ้งล่วงหน้า และเกิดขึ้นกับ
 * โปรเจกต์นี้มาแล้ว 2 ครั้ง**
 *   - ก.ค. 2026 — `meta-llama/llama-4-scout-17b-16e-instruct` (โมเดลดูรูป) หายไป
 *   - ส.ค. 2026 — `llama-3.3-70b-versatile` (โมเดลข้อความหลัก) หายไป ทำให้ทั้ง
 *     /api/suggest-menus, /api/generate-recipe และ /api/chat ตายพร้อมกันทั้ง 3 ตัว
 *
 * เดิมชื่อโมเดลถูกฮาร์ดโค้ดกระจายอยู่ใน route ทั้ง 4 ไฟล์ เวลาโดนถอดทีต้องไล่แก้ทีละที่
 * และหาไม่เจอว่าตกหล่นตรงไหน — รวมไว้ตรงนี้แล้วเปลี่ยนที่เดียวจบ
 *
 * 🔧 วิธีแก้เมื่อเกิดอีก (สังเกตจาก error `model_not_found` หรือ 404 จาก Groq):
 *   1. ดูรายชื่อโมเดลที่ยังใช้ได้: `curl https://api.groq.com/openai/v1/models -H "Authorization: Bearer $GROQ_API_KEY"`
 *   2. เปลี่ยนค่าในไฟล์นี้ให้เป็นตัวที่ยังอยู่
 *   3. ทดสอบภาษาไทย + JSON mode ก่อนเสมอ — โมเดลที่ตอบอังกฤษดีไม่ได้แปลว่าตอบไทยดี
 *      และโมเดลตระกูล reasoning ต้องตั้งค่าปิดส่วน "ความคิด" ไม่งั้นจะหลุดปนมาในคำตอบ
 */

/**
 * โมเดลข้อความ — ใช้ใน /api/suggest-menus, /api/generate-recipe, /api/chat
 *
 * ตอบภาษาไทยสะอาด (ไม่ปนคำภาษาอื่นกลางประโยค) และรองรับ `response_format: json_object`
 *
 * ⚠️ ข้อควรระวังที่เจอตอนทดสอบ: บางครั้งโมเดลนี้แทรก element ขยะ (สตริงว่าง `""`)
 * คั่นกลาง array ที่ตอบกลับมา ซึ่งเป็น JSON ที่ parse ผ่านแต่ Zod จะตีตกทั้งชุด
 * ทุกที่ที่รับ array จากโมเดลนี้จึงต้องกรอง element ที่ไม่ใช่ object ทิ้งก่อน validate เสมอ
 */
export const TEXT_MODEL = 'openai/gpt-oss-120b';

/**
 * โมเดลดูรูป — ใช้ใน /api/vision
 *
 * ⚠️ เป็น reasoning model ต้องตั้ง `reasoning_format: 'hidden'` เสมอ ไม่งั้นจะแทรก
 * `<think>...</think>` ปนมาในคำตอบ แล้วโดน `.split(',')` สับเป็น "วัตถุดิบ" ปลอมๆ แบบเงียบๆ
 */
export const VISION_MODEL = 'qwen/qwen3.6-27b';
